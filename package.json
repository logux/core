{
  "name": "logux-core",
  "version": "0.0.0",
  "description": "Logux core",
  "keywords": [],
  "author": "Andrey Sitnik <andrey@sitnik.ru>",
  "license": "MIT",
  "repository": "logux/logux-core",
  "dependencies": {},
  "devDependencies": {
    "docdash": "^0.4.0",
    "eslint": "^3.5.0",
    "eslint-config-standard": "^6.0.0",
    "eslint-plugin-promise": "^2.0.1",
    "eslint-plugin-standard": "^2.0.0",
    "jest": "^15.1.1",
    "jsdoc": "^3.4.1",
    "lint-staged": "^3.0.2",
    "pre-commit": "^1.1.3",
    "rimraf": "^2.5.4"
  },
  "scripts": {
    "lint-staged": "lint-staged",
    "clean": "rimraf docs/ coverage/",
    "lint": "eslint *.js test/*.js",
    "docs": "jsdoc *.js --template node_modules/docdash --destination docs/",
    "test": "jest --coverage && npm run lint && npm run docs"
  },
  "jest": {
    "coverageThreshold": {
      "global": {
        "statements": 100
      }
    }
  },
  "eslintConfig": {
    "extends": "standard",
    "rules": {
      "valid-jsdoc": "error",
      "no-new": "off"
    },
    "env": {
      "jest": true
    }
  },
  "lint-staged": {
    "*.js": "eslint"
  },
  "pre-commit": [
    "lint-staged"
  ]
}
